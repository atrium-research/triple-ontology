# IDE
.idea

# AI Assistant Instructions
CLAUDE.md
# .claude/skills/ is versioned: it carries the project methodology.
# Only local, machine-specific state is ignored.
.claude/settings.local.json

# External mapping specs (work in progress, not yet versioned)
mapping_spec/

# Local metadata (synced GitHub issues, notes)
_meta/

NOTES.md


base_model.md


env
venv

build